services:
  - type: web
    name: painel-ehf-brasil
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:server --timeout 120"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
